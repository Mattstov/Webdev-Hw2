# Expense Tracker

A personal expense tracker built with vanilla HTML, CSS, and JavaScript.

## Usage

Clone the repo and open `index.html` in a browser.

## Stack

Plain HTML / CSS / JS — no frameworks, no build step.
